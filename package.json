{
  "scripts": {
    "build": "npx tsc",
    "build:production": "rm -r ./out; npx tsc --sourceMap false",
    "run": "node ./out",
    "deploy": "sftp -b deploy.bat raspi"
  },
  "devDependencies": {
    "@types/async-lock": "^1.1.5",
    "@types/node": "^16.10.2",
    "prisma": "^5.2.0",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@discordjs/builders": "^0.6.0",
    "@prisma/client": "^5.2.0",
    "async-lock": "^1.3.2",
    "discord.js": "^13.16.0"
  }
}
